extends layout


block content
  for board in portInfo
    h2 Item at 
      i #{board.comName}
    p this is too

block paths
  small  > 
    a(href='/config') Configure
  small  > 
    p Port


block scripts
  script(src="/javascripts/each.js")
